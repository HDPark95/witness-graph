.venv/
quickstart.log
__pycache__/
*.pyc

# Seeded warehouses are rebuilt from the cases, so the bytes are derived data:
#   python3 harness/warehouse.py --all --out warehouse/
warehouse/
*.db

# Scratch logs from batch runs.
/tmp-*.log

# The evidence site is rebuilt from the ledgers, so the bytes are derived data:
#   python3 docs/site/build.py --out _site
_site/
